name: tcc_attendance_app
description: 'A new Flutter project.'

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  flutter_beacon: ^0.5.1
  get: ^4.6.6
  permission_handler: ^11.3.1
  app_settings: ^5.1.1
  progress_dialog_null_safe: ^3.0.0
  google_fonts: ^6.2.0
  flutter_blue_plus: ^1.31.16

dev_dependencies: 
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.1

flutter:
  uses-material-design: true

flutter_native_splash:
  color: '#182026'
  image: assets/logos/splash_screen_logo.png

  android_12:
    color: '#182026'
    icon_background_color: '#182026'
    image: assets/logos/splash_screen_logo_big.png

  web: false
